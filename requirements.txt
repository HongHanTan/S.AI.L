# Web runtime. Vercel installs exactly this.
#
# The inbox screens read a precomputed run.json, but the Compare tab runs the
# real extraction and comparison pipeline on uploaded documents, so the parsers
# ship too. google-genai is deliberately absent: the upload path is
# deterministic, so the deployed demo makes no model calls and cannot be
# broken by an API quota.
fastapi>=0.115
pydantic>=2.0
python-multipart>=0.0.9
pymupdf>=1.24
python-docx>=1.1
openpyxl>=3.1
