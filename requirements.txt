# Web runtime only — this is what Vercel installs for the deployed demo.
# The server reads a precomputed run.json and never parses documents, so the
# heavy parsing and model libraries stay out of the deployment bundle.
fastapi>=0.115
pydantic>=2.0
