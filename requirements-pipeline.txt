# Everything needed to RUN the pipeline and the test suite locally.
-r requirements.txt

pymupdf>=1.24            # PDF text extraction
python-docx>=1.1         # .docx paragraphs and tables
openpyxl>=3.1            # .xlsx label/value sheets
google-genai>=0.3        # Gemini classification, fallback extraction, L4
uvicorn>=0.30            # local web server
jinja2>=3.1
google-cloud-firestore>=2.16  # optional: persists review decisions
httpx>=0.27              # FastAPI TestClient
pytest>=8.0
